#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>

char* getInput(int argc, char *argv[])
{ 
	if (argc < 2)
	{
		return ;
 	}
	printf("%s\n", argv[1]);
	return argv[1];
}
